# F13 Chat Interface — Audit
> Re-audited: 2026-08-13 (second pass, compares against prior audit of same date)
> **Scoring note**: The user has confirmed Qubere is intentionally sticking with Gemini as the chat/advisory model — Claude is NOT a requirement. Task A-5 and the model-choice portion of D-1 are scored as satisfied by design; they are no longer counted as gaps. The Anthropic SDK integration exists in the code as a working, tested alternate path but is not required to be active.
>
> **Addendum, 2026-08-22 (not a full re-pass — targeted notes only)**: two changes landed since the second pass above that this file has not been fully re-scored against. (1) The dead, parallel `src/modules/copilot/` tool registry referenced throughout this audit (see B-1, item 4 under Quality Standards) is now deleted outright, along with every `tests/copilot-*.test.ts` file — the "unused `copilot-*.test.ts` files ... unchanged/still present" note under Quality Standard #4 below is now stale. (2) The live registry in `src/modules/assistant/tools.ts` grew from 27 tools (this audit's count) to **41**, via 11 new tools that reimplement the capabilities the deleted `copilot/` registry uniquely held (product/party history and evidence, shipment filing readiness, a task queue, country embargo screening, and restricted-party screening/rescreening — the last gated by a real `access.permission` check and covered by a new `tests/assistant-tools-rbac.test.ts` regression test). None of these 11 are among the specific B-2 (11 read-only) or B-3 (8 write) tools this audit scores against, so the **17 of 19 spec'd tools** figure and the B-2/B-3 rows below are unchanged and still accurate — this is bonus coverage outside the F13 spec, not a change to spec conformance. A separate, unrelated fix landed the same day: the daily/on-demand compliance audit's `hasBrokerApproval` check, previously always-false because it searched `AuditLog` for an action string (`"filing.approve"`) no route ever wrote, now reads `CustomsFiling.approvedByUserId` directly — out of scope for this file (it lives in Group M/Q of the platform-wide audit, not F13), noted here only for cross-reference.

Overall readiness: **~52%** (previously 32%; the 48% figure from this same re-audit pass has been revised upward now that the Claude requirement is removed)

Tool coverage: **17 of 19** spec'd tools implemented (unchanged from previous audit — `upload_document` and `create_scenario` are still not model-callable tools). The registry grew from 24 to **27** total tools (3 bonus tools added: `get_classification_rationale`, `get_duty_exposure_risks`, `validate_shipment_filing`, covering D-2/D-3/D-4), but neither of the two spec-required missing write tools was added.

**Headline claim check — "All UI actions must be doable via chat" is still FALSE today**, though real ground was covered. Genuine fixes: Zod runtime validation replaced trusted type-casts on tool args; `templateFallback()` (the silent-canned-boilerplate bug) is gone; 23 of 27 tools now render structured cards, including real inline Approve/Reject/Resolve buttons; D-2 and D-4 (HTS rationale, filing readiness) went from entirely unimplemented to fully wired; the `reject_decision`/`generate_reasonable_care_record`/`run_impact_analysis` permission-gate bugs are fixed; real Vitest coverage now exists for the shipped path and passes. Remaining, adversarially-verified problems that are NOT related to model choice: (1) the new `AuditLog.source` column is **never actually set to `"CHAT"`** by any route the chat write-buttons call — `decisions/route.ts:427` and `exceptions/[id]/route.ts:64` both hardcode `source: "UI"` regardless of the `x-qubere-source: CHAT` header and `source: "CHAT"` body field the client faithfully sends (94 hardcoded `"UI"` call sites repo-wide, zero that read the header/body signal) — so provenance tracking looks fixed but is functionally identical to before; (2) the new inline "Resolve Exception" button in chat (`ChatClient.tsx:1489-1499`) omits the server-required `expectedVersion` field, so every click 400s server-side, yet the code unconditionally shows `alert("Exception resolved.")` — this button is **guaranteed broken** on every use, and the Approve/Reject buttons have the identical no-`res.ok`-check pattern, so any real failure (permission, stale version) is silently reported to the user as success; (3) the new "duty exposure risk" tool computes `NaN` for essentially every classified line item due to a duty-rate parsing bug.

---

## Capability A — Streaming Chat API

| Task | Status | Change | Evidence | Gap / Fix needed |
|---|---|---|---|---|
| A-1: SSE streaming, spec'd event shape | PARTIAL | UNCHANGED (+ new bug) | `src/modules/assistant/orchestrator.ts:90-96` still emits `{type:"text"}` not `{type:"text_delta"}`, and the unspec'd `{type:"history"}` event still exists. **New**: the Anthropic branch (`orchestrator.ts:166-266`) never yields a `history` event at all — only the Gemini fallback branch does (`orchestrator.ts:348,356`). | Since `ChatClient.tsx:666-708` only updates `workingHistory` on receipt of a `history` event, and persists that as the session's `history` field, a session driven by Claude never accumulates conversation turns — every subsequent Claude turn is reconstructed from stale/empty history (`orchestrator.ts:178-186`). Multi-turn memory is silently broken specifically when Claude is active, directly contradicting the system prompt's own claim ("You persist and remember all previous messages," `orchestrator.ts:23-26`). |
| A-2: `GET/DELETE /api/assistant/chats[/[id]]`, pagination | PARTIAL, IMPROVED | FIXED (pagination) / UNCHANGED (detail GET missing) | Real cursor-based pagination now exists: `src/app/api/assistant/chats/route.ts:17-26` reads `cursor`/`limit`/`pageSize` query params, clamps `limit` to 1-50, uses `take: limit+1` + `cursor`/`skip` for a proper next-page cursor. | `GET /api/assistant/chats/[id]` (session detail) still does not exist — `src/app/api/assistant/chats/[id]/route.ts` only exports `PATCH` (line 16) and `DELETE` (line 43), confirmed by full file read. |
| A-3: session context `{accountId, clientId?, shipmentId?, documentId?}` | MISSING | UNCHANGED | `ChatTurnInput` (`orchestrator.ts:84-88`) still has only `message`, `history`, `requestId`. `grep -n "shipmentId\|clientId\|documentId" orchestrator.ts` matches only a system-prompt sentence telling the model never to trust a user-typed id, not an actual context field. | Whole capability still absent — no way to scope a session to a shipment. |
| A-4: persist to `AssistantChatMessage` rows | MISSING | UNCHANGED (+ newly broken under Claude, see A-1) | `prisma/schema.prisma:3689-3703` — `AssistantChatSession` still stores `messages Json` / `history Json` directly; `AssistantChatMessage` model still does not exist (`grep -c "model AssistantChatMessage" prisma/schema.prisma` → 0). Schema comment at line 3698 still says "Gemini `Content[]` turn history" — the persistence design is still Gemini-shaped even though Claude is now a live code path. | Same structural gap as before, now compounded by the A-1 bug: for Claude-driven sessions this Json blob doesn't even capture the turns correctly. |
| A-5: switch chat model to Claude | **N/A — superseded by product decision** | REQUIREMENT REMOVED | `orchestrator.ts:1-16` imports both `GoogleGenAI` and `Anthropic`, branching on `useAnthropic = Boolean(process.env.ANTHROPIC_API_KEY)` (line 116) — Gemini is the active path with no key configured, which is now the intended, permanent configuration, not a gap. The Anthropic branch (`orchestrator.ts:166-266`) remains in the codebase as a working, tested optional path but activating it is no longer a goal. | None — do not provision `ANTHROPIC_API_KEY` for this purpose. If the Anthropic code path is not going to be used going forward, consider removing it to cut maintenance surface (it currently has its own multi-turn-memory bug under A-1 that only matters if the branch is ever activated); otherwise leave as an optional path with no action required. |
| A-6: Vitest — streaming order, session scoping, shipment context | **FIXED** | FIXED | `tests/assistant-orchestrator.test.ts` (66 lines) now exists and passes: verifies the "no API key configured" error event, and that `runAssistantTurn` returns a valid async generator when `ANTHROPIC_API_KEY` is set. Ran `npx vitest run tests/assistant-orchestrator.test.ts tests/assistant-tools.test.ts tests/assistant-advisory.test.ts` → **3 files, 9 tests, all passed.** | Coverage is thin: the "Anthropic configured" test (`assistant-orchestrator.test.ts:54-65`) only asserts `gen[Symbol.asyncIterator]` is defined — it never actually iterates the generator or mocks the Anthropic SDK, so it would not catch a broken Anthropic call. No test covers event ordering, session-scoping, or shipment context (because A-3 doesn't exist to test). Still a real, non-trivial improvement over zero. |

## Capability B — Tool Definitions (API as Tools)

| Task | Status | Change | Evidence | Gap / Fix needed |
|---|---|---|---|---|
| B-1: tool registry, Zod input schemas | **FIXED** | FIXED | Every tool now defines a real Zod `schema` and calls `schema.safeParse(rawArgs)` before executing (24 of 27 tools use this pattern, confirmed via `grep -c "safeParse(rawArgs)" tools.ts` → 24; remaining 3 take no args). `grep -n "rawArgs as " tools.ts` → **zero matches** — the previously-flagged trusted type-assertion pattern is gone. | None of substance. Declarations are still converted to Gemini-native `Schema`/`Type` via `zodToGeminiSchema()` (`tools.ts:30`) for use with both providers, which is a reasonable compromise, not a defect. |
| B-2: 11 read-only tools | DONE | UNCHANGED | All 11 still present and confirmed by name in `ASSISTANT_TOOLS` (`tools.ts:1177-1205`). | None. |
| B-3: 8 write tools with permission checks | PARTIAL | IMPROVED (permission bugs fixed) / UNCHANGED (still 6/8 tools) | Still only 6/8 spec'd write tools exist: `upload_document` and `create_scenario` remain absent as model-callable tools (`grep -rn "create_scenario" src/` → zero; `upload_document` only appears as client-side display/simulation code in `ChatClient.tsx`, never in `tools.ts`'s `ASSISTANT_TOOLS`). **Fixed**: the previously-flagged `reject_decision` permission mismatch is corrected — it now declares `access: { permission: "decisions.reject" }` (`tools.ts:905`), matching the route's real requirement. `generate_reasonable_care_record` now has `access: { permission: "documents.read" }` (`tools.ts:485`) and `run_impact_analysis` now has `access: { permission: "regulatory.review" }` (`tools.ts:851`) — both previously ungated. | `create_scenario`/`LandedCostScenario` chat path still doesn't exist (`src/app/api/simulator/scenarios/route.ts` exists in the UI but no chat tool calls it). `upload_document` is still simulated entirely client-side (`ChatClient.tsx:788-896`) — the model still never calls a real tool for it. |
| B-4: tool-call rendering (chip → structured card) | PARTIAL, substantially improved | **FIXED, mostly** | `ToolCard()` in `ChatClient.tsx:1245-1631` now renders bespoke cards for 23 of 27 tools (`get_shipment`, `get_duty_stack`, `list_decisions`/`approve_decision`/`reject_decision` with inline buttons, `list_exceptions`/`resolve_exception` with inline button, `get_document`, `get_product`/`classify_product`, `get_filing_status`/`validate_shipment_filing`, `run_impact_analysis`, `generate_reasonable_care_record`, `get_classification_rationale`, `get_duty_exposure_risks`, plus the 8 previously covered) — up from 8 of 24. This is genuine, verified progress; the founder's named card types (decision card with actions, exception card with resolve, duty stack card) now exist. | Still `return null` (`ChatClient.tsx:1631`) for `search_hts`, `search_rulings`, `get_regulatory_updates`, `export_compliance_record` — same 4 gaps as before, unaddressed. **New bug**: none of the inline action buttons (`Approve`/`Reject`/`Resolve Exception`, `ChatClient.tsx:1437-1499`) check `res.ok` before showing a success `alert(...)` — a failed request (permission denied, stale version, validation error) is reported to the user as a success. **New bug, more severe**: the `Resolve Exception` button (`ChatClient.tsx:1489-1499`) never sends `expectedVersion`, which `updateSchema` in `src/app/api/exceptions/[id]/route.ts:24` requires with no `.optional()` — every click is guaranteed to fail server-side validation (400), while the UI unconditionally alerts "Exception resolved." This button is broken on every use as shipped. |
| B-5: Vitest — tenant isolation (404 not 403), permission-gated errors | MISSING | UNCHANGED | Same as before: underlying delegated routes correctly scope by `accountId` (inherited behavior), but no test in the new `tests/assistant-*.test.ts` files directly exercises this for `src/modules/assistant/tools.ts` (`grep -n "404\|403\|tenant\|foreign" tests/assistant-*.test.ts` → zero). | Add a direct cross-tenant/permission-denied test for at least one write tool. |

## Capability C — Chat UI

| Task | Status | Change | Evidence | Gap / Fix needed |
|---|---|---|---|---|
| C-1: chat panel + context panel (shipment/docs/exceptions, selectable) | MISSING | UNCHANGED | `RightPanel()` (`ChatClient.tsx:447-538`) is still just dark-mode toggle, "Try asking" sample prompts, and the session list — read in full, confirmed no shipment/document/exception panel exists. | Whole capability absent, same as before. |
| C-2: structured domain result cards | PARTIAL, substantially improved | **FIXED, mostly** — same evidence as B-4 | Duty stack card, decision card (with Approve/Reject), exception card (with Resolve), filing readiness card, and classification-rationale card now all exist and are wired to real data. | 4 tools still lack cards (see B-4). |
| C-3: inline actions + `AuditLog.source:"CHAT"` | PARTIAL — **looks fixed, functionally isn't** | Structural FIX / functional UNCHANGED | Real progress: `AuditLog` now has a `source` column (`prisma/schema.prisma:347`, `source String @default("UI")`, indexed at line 357) and `createAuditLog()`'s type signature documents `AuditSource = "UI" \| "CHAT" \| "SYSTEM" \| "API"` (`src/lib/audit.ts:7,27`). The chat buttons and every chat write-tool dutifully send `x-qubere-source: CHAT` header and `source: "CHAT"` body field (`ChatClient.tsx:1441-1494`; `tools.ts:881-1037`, 9 call sites). | **But nothing on the server ever reads it.** `grep -rln "x-qubere-source" src/app/api/` → zero files read this header. `src/app/api/decisions/route.ts:427` and `src/app/api/exceptions/[id]/route.ts:64` both hardcode `source: "UI"` in their `createAuditLog(...)` calls, ignoring the request body's `source` field entirely (it isn't even destructured in `decisions/route.ts:249`). `grep -c 'source: "UI"' src/` → 94 hardcoded occurrences repo-wide; `grep -rn 'source:.*(req\|header\|body\.source\|x-qubere)'` → zero dynamic assignments anywhere. Only the unused `copilotAudit.ts` module (never called from a live route) correctly sets `source: "CHAT"` (lines 98,146,180,200,217). **A chat-originated approval/rejection/resolution is still indistinguishable from a UI one in the audit trail today** — the schema was extended but the wiring that would make it true was never done in the routes that matter. This is the single most important "looks done, isn't" finding in this re-audit. |
| C-4: multi-part file upload → Vercel Blob → `ShipmentDocument` | PARTIAL | UNCHANGED | Real upload flow unchanged: `performUpload()` posts to `/api/documents/upload`, polls processing/extraction status. | Still always requires a resolved shipment before upload (`orchestrator.ts:67-76`, "FILE ATTACHMENTS" section unchanged); no "unattached document" path exists, same gap as before. |
| C-5: context persistence across messages, clear command, shown in header | MISSING | UNCHANGED | Still depends on A-3, which doesn't exist. No "clear context" command found. | Whole capability absent. |
| C-6: session sidebar grouped Today/Yesterday/Last 7 days; 60-char title | PARTIAL | UNCHANGED | Auto-title still truncates to **50** chars (`ChatClient.tsx:655`, `trimmed.slice(0, 50)`), not the spec'd 60. Still a flat list with per-item relative timestamps (`relativeTime()`), no grouped headers. | Same two gaps as before, neither addressed. |

## Capability D — Chat as Compliance Interface

| Task | Status | Change | Evidence | Gap / Fix needed |
|---|---|---|---|---|
| D-1: advisory query refactor (model choice N/A, see scoring note) + account-specific citations | PARTIAL | IMPROVED (Quality Standard fix) / UNCHANGED (context depth) | `src/app/api/advisory/query/route.ts` runs on Gemini by default, matching the product decision to stay on Gemini; the dormant Anthropic branch is present but not required. **`templateFallback()` is completely gone** (`grep -rn "templateFallback" src/` → zero hits) — a real, verified fix for the previously-flagged Quality Standard #1 violation. On any model failure, the route now returns an honest `"Advisory analysis is temporarily unavailable due to a service error."` (line 148) instead of canned boilerplate indistinguishable from a real answer. | Context injection is still only aggregate account metrics (`computeAnalyticsMetrics`, lines 72,83-89) + 5 recent regulatory updates — no product/shipment/ruling-level detail as the spec requires, same gap as before. Citations are still generic, not account-specific rulings/HTS codes/documents. This gap is independent of model choice. |
| D-2: "Why was this HTS code chosen?" | **FIXED** | FIXED | New tool `get_classification_rationale` (`tools.ts:1058-1076`) calls the real `classification-rationale` route in-process and returns the actual `ClassificationCase` GRI steps and ruling citations. `ChatClient.tsx:1586-1608` renders a dedicated card showing GRI steps and CBP ruling citations. | None found — this capability is genuinely implemented now. |
| D-3: "What are my duty exposure risks?" | PARTIAL — real feature, real bug | FIXED (structurally) / **new bug found** | New tool `get_duty_exposure_risks` (`tools.ts:1084-1132`) queries real `shipmentLineItem` rows scoped to `accountId`, ranks by estimated duty, returns top-N. This is genuine new functionality where previously nothing existed. `ChatClient.tsx:1610-1629` renders a real card. | **Duty-rate parsing bug**: `tools.ts:1107` computes `const rate = hts ? Number(hts.generalDutyRate ?? 0.05) : 0.05;` — but `generalDutyRate` is a raw text string like `"5%"` or `"Free"` (`prisma/schema.prisma` `rawRateText`/`dutyEngine.ts:299`), and `Number("5%")` evaluates to `NaN` (confirmed via `node -e`). The codebase's own correct parser, `parsePublishedDutyRate()` (`dutyEngine.ts:313-319`, imported into the same file), is not used here. In practice, every line item with a real HTS-assigned duty rate produces `estimatedDuty = NaN`, so the "top-3 risks by dollar value" feature likely shows `$NaN` or garbage ordering for any account with real classified shipments — only the fallback (unclassified) case coincidentally works. Also uses plain JS `number` arithmetic rather than Decimal.js (Quality Standard #2), and never incorporates regulatory updates as the spec text specifies. |
| D-4: "Is my shipment ready to file?" | **FIXED** | FIXED | New tool `validate_shipment_filing` (`tools.ts:1141-1175`) resolves a filing from a shipment/filing id and calls the real `filingValidator` route in-process. `ChatClient.tsx:1537-1562` renders blockers/warnings in plain English with a "View filing center" link. | None found — this capability is genuinely implemented now. |
| D-5: question routing (simple lookup vs. reasoning chain) | PARTIAL | UNCHANGED | Same single bare-HTS-code regex shortcut (`advisory/query/route.ts:14,40-50`) as before; every other question still goes through one undifferentiated model call. | No broader routing added. |
| D-6: rate limiting via `AiUsageWindow`/`aiQuotaGate` | DONE | UNCHANGED | Still functional; still surfaced through a generic error wrapper in chat rather than a dedicated "AI quota reached" UI state (`grep -n "quota" ChatClient.tsx` → zero matches). | Minor cosmetic gap, unchanged. |

## Cross-cutting Quality Standards violations found

1. **No fake data, ever (#1)** — FIXED for the previously-flagged `templateFallback()` case (removed). **New violation found**: `get_duty_exposure_risks` (`tools.ts:1107`) silently produces `NaN`/garbage dollar figures for real data due to a rate-parsing bug — presented in a polished card (`ChatClient.tsx:1610-1629`) as if it were a real number, which is arguably worse than an honest empty state.
2. **Money is always Decimal.js (#2)** — new violation: `get_duty_exposure_risks` (`tools.ts:1104-1120`) does duty math with plain JS `number`, not `Decimal`, unlike the codebase's established `dutyEngine.ts` pattern it sits next to.
3. **Tenant isolation (#3)** — still generally respected via delegated routes; `run_impact_analysis` and `generate_reasonable_care_record` now have explicit `access` gates (fixed, see B-3), closing the inconsistency flagged last time.
4. **Vitest per capability (#4)** — FIXED for the shipped path: `tests/assistant-orchestrator.test.ts`, `tests/assistant-tools.test.ts`, `tests/assistant-advisory.test.ts` now exist and pass (9 tests, verified via `npx vitest run`). Coverage is thin (no mocked-Anthropic-response assertion, no B-5 tenant-isolation test) but real. The unused `copilot-*.test.ts` files (testing dead code) are unchanged/still present alongside.
5. **Every write goes to `AuditLog` (#5)** — writes are still audited, but the specific, named requirement of this feature (`source: "CHAT"` distinguishing chat-originated writes) is **still not functionally true** despite the schema now supporting it — see C-3 above. This is the most consequential "looks fixed, isn't" finding in this pass.
6. **No `any` types (#7)** — substantially improved: the previous `rawArgs as X` casts in `tools.ts` are gone, replaced by real Zod `safeParse`.
7. **Pagination on all list endpoints (#8)** — FIXED for `GET /api/assistant/chats` (cursor-based, capped at 50).
8. **New: unchecked response status before user-facing success confirmation** — not an enumerated Quality Standard but a serious data-integrity smell: `ChatClient.tsx:1437-1499`'s three new inline action buttons all call `fetch()` and immediately `alert("...successfully")` without checking `res.ok`, so failures are misreported as successes to the user — directly undermining the product's "provable audit trail" positioning.

## Top 5 fixes ranked by severity

1. **Wire the `source` signal the client already sends into the routes that matter.** `decisions/route.ts:427` and `exceptions/[id]/route.ts:64` (and `productService.ts`'s ~5 call sites) hardcode `source: "UI"` even though the chat UI and every chat write-tool already send `x-qubere-source: CHAT` / `source: "CHAT"`. This is a small, mechanical fix (read `body.source` or the header, pass it through to `createAuditLog`) that closes the single most-cited gap from the prior audit — right now it only looks closed.
2. **Fix the `Resolve Exception` chat button, and add `res.ok` checks to all three inline action buttons.** `ChatClient.tsx:1489-1499` omits the server-required `expectedVersion`, guaranteeing a 400 on every use while telling the user it succeeded. Approve/Reject have the same silent-failure pattern. This is currently a demo-breaking bug in newly-shipped, user-facing code.
3. **Fix the duty-rate parsing bug in `get_duty_exposure_risks`.** Swap `Number(hts.generalDutyRate ?? 0.05)` for the existing `parsePublishedDutyRate(hts?.generalDutyRate)` helper already imported into `tools.ts` via `dutyEngine.ts`, and use Decimal.js for the arithmetic. Without this, D-3's headline "duty exposure risk ranking" feature returns `NaN` for real accounts.
4. **Build A-3 session context (`shipmentId`/`clientId`/`documentId`) and the C-1 context panel.** This remains the largest structurally-missing capability — without it, chat cannot be scoped to "this shipment's workspace," which is central to the plan's "second surface for the UI" framing.
5. **Fix the A-1 multi-turn-memory bug only if the dormant Anthropic branch is ever activated** (low priority since Gemini is the confirmed permanent choice) — otherwise consider removing the unused Anthropic branch to reduce maintenance surface, since it is not required and currently carries its own untested bug.
