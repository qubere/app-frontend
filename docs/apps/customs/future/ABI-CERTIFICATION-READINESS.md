# ABI Certification Readiness Plan
> Created: 2026-08-20. Updated: 2026-08-20 — direct-transmission path confirmed; full chapter build-out confirmed (not the P0-only staged approach originally recommended). Scope decision: Qubere pursues direct CBP ABI transmission (not the filer-handoff model). This **reverses** the non-goal stated in `docs/requirements/7501-draft-and-abi-export.md:3-5` ("Qubere does not transmit to CBP. The filer does.") — that doc's serializers (U8-U10) become useful groundwork, not the finish line.

**Source documents:** downloaded live from cbp.gov into [`docs/plans/catair-source-docs/`](catair-source-docs/) — see §4. (Our WebFetch tool specifically got 403'd by CBP's bot protection; a plain browser-UA `curl` was not blocked, so the docs were pulled directly rather than reconstructed from search.)

## 0. What CBP is actually going to test

CBP's "ABI test" is not a written exam — it's a certification process run by an assigned **CBP Client Representative**, who supervises live test transmissions into CBP's ACE test environment until your software reliably produces valid, accepted transactions for each transaction type you intend to file in production. It is gated on:

1. A **Letter of Intent** to CBP identifying who you are and what you intend to transmit.
2. Legal/operational standing to file — see [Open Question A](#open-question-a-who-is-the-filer-of-record) below; this determines whether you need a licensed customs broker on staff, a continuous bond, and/or a filer code of your own.
3. Software that produces CATAIR-conformant records, wrapped in CBP's batch/block envelope, over an approved transport (VAN, AS2, or CBP-approved direct connect).
4. Passing CBP's test scenarios for each chapter in scope — CBP does **not** require every CATAIR chapter, only the ones matching your declared transaction types.
5. Sustained production performance under 19 CFR 143 Subpart A once live — CBP can suspend ABI privileges for a bad error rate. This is a standing obligation, not a one-time gate.

**Caveat on chapter list currency:** CBP's site (`cbp.gov/trade/automated/catair`) blocks automated fetching (403 on every path tried, including PDFs and the Wayback snapshot), so the chapter list below is reconstructed from search-indexed document titles/dates, not a live read of the page. CATAIR chapters revise often — we saw Entry Summary Create/Update revisions dated 2016, 2024-09, 2025-01, 2025-06, and a "future" 2026-07 Entry Summary Query draft in search results alone. **Once a Client Representative is assigned, get the current authoritative chapter list and revision numbers from them directly** — do not build against what's below without that confirmation.

---

## Open Question A: who is the filer of record?

**Resolved 2026-08-20: Qubere transmits to CBP directly.** That confirms the *technical* path — real transport, real credentials, no more handing files to a third-party filer.

**Resolved 2026-08-21: software-vendor path, not self-filer.** Qubere's customers are themselves brokers — Qubere transmits to CBP on their behalf, under each broker-customer's own filer code/credentials, not as the filer of record itself. This is the **ABI Software Vendor** path from the table below, not the self-filing path. Practical effect: no continuous bond or in-house licensed customs broker required for Qubere itself (19 CFR 111's broker-license requirement attaches to the filer of record — the customer-broker — not to Qubere as the software vendor); CBP's intake is software-certification paperwork, not bond/broker paperwork; Qubere still needs to certify against each CATAIR chapter it transmits, and each customer-broker's own credentials/liability apply per-transmission. The engineering build (envelope, serializer, transport, response parsing) is unaffected by this — same either way — only the Phase 0 paperwork and the credential model in `RealAceProvider` (per-customer filer credentials, not one Qubere-wide credential) are.

Letter of Intent / initial outreach already sent to CBP as of 2026-08-21 — awaiting response.

<details>
<summary>Original two-path comparison (for reference — path is now decided)</summary>

| Path (both are "direct transmission" from CBP's perspective) | What it requires | Implication |
|---|---|---|
| Qubere self-files (Qubere itself is the ABI filer of record) | A continuous bond, a filer code issued to Qubere, and — per 19 CFR 111 — a licensed customs broker on staff if filing on behalf of customers (not just Qubere's own imports) | Qubere takes on legal filing liability for every transmission. |
| **Qubere certifies as an ABI software vendor** (chosen), transmitting under each customer's own filer code/credentials | Software-level CBP certification; each customer still holds their own filer code and legal responsibility | Still genuinely "direct transmission" (no third-party filer in the loop) — the difference is whose credentials/liability it runs under. Matches CBP's [ABI Software Vendors list](https://www.cbp.gov/document/guidance/abi-software-vendors-list) model. |

</details>

---

## 1. Current codebase state vs. what direct transmission requires

Grounded in the 2026-08-13 audit (`docs/plans/review/OPEN-ITEMS.md`) plus direct file reads.

| Capability | Current state | File(s) | Gap for real ABI transmission |
|---|---|---|---|
| Entry Summary data model (7501) | Real, provenance-tracked field builder | `src/lib/filing/form7501.ts` | None at data-model level — this is solid ground to build on |
| 7501 PDF export | Exists (contradicts OPEN-ITEMS #17, which predates this file by hours) | `src/lib/filing/form7501Pdf.ts` | Not CATAIR-relevant, but confirms the field data is real |
| CATAIR-shaped EDI serializer | **Spec'd, not built.** Existing spec (U9 in `7501-draft-and-abi-export.md`) intentionally targets a "broker's proprietary flat file," not raw CBP-conformant CATAIR | `docs/requirements/7501-draft-and-abi-export.md:444-475` | Must be rescoped: fixed-length record layout must match CBP's actual published field positions, not an internal reference layout |
| Batch & Block Control envelope (CBP's header/trailer wrapper required on every transaction) | Does not exist | — | Net-new. Every CATAIR chapter transaction rides inside this envelope; build once, reuse everywhere |
| Real ACE/ABI transmission provider | **Stub only** — every method throws "not yet implemented" | `src/lib/filing/transmissionProvider.ts` (`RealAceProvider`) | Net-new: actual connection (VAN/AS2/direct), CBP account credentials, real request/response cycle |
| CBP response/reject handling | Fully simulated (`devStub.ts`), explicitly dev-only | `src/lib/canonicalMessaging/devStub.ts:1-21` | Net-new: parse CBP's real response/reject transaction codes, replace the simulation, keep the queue/audit infrastructure around it (that part is real and reusable) |
| Message routing/config (procedure codes, message catalog, most-specific-match resolution) | Real, well-tested, country-agnostic | `src/lib/canonicalMessaging/resolveMessageContext.ts`, `wildcardLookup.ts` | Reusable as-is — this is legitimately good infrastructure for the certification build |
| Cargo Release | Not modeled | — | Net-new if in scope (see chapter table below) |
| Manifest | Not modeled at all — "manifest" only appears in doc-intake keyword lists | — | Out of scope unless Qubere intends to file as a carrier, which nothing in the product suggests |
| In-Bond | Entry-type code recognized (`61`/`62`/`63`); no structured transaction | `src/modules/filing/entryType.ts:29` | Build if declared in scope |
| Statement Processing (payment) | Not modeled | — | Net-new, required for any production filer (this is how duties actually get paid) |
| Bond / eBond | Basic CRUD, `status: "Unverified"` — no real surety/ACE verification | `src/modules/bonds/bond.service.ts` | Needs real bond verification against CBP/surety data before certification, not just a status field |
| Drawback (TFTEA) | Real claim/match/lot models and service | `src/modules/drawback/drawback.service.ts`, Prisma `DrawbackClaim` etc. | Good foundation; needs a CATAIR drawback chapter transaction on top |
| PGA screening | Real screening logic and API | `src/app/api/pga/screen/route.ts`, `HtsPgaRequirement` model | Screening ≠ filing; the PGA Message Set chapter is a distinct transaction format still to build |
| AD/CVD rate data | Architecture real, data "almost unseeded" (1 AD row, 0 CVD rows) | OPEN-ITEMS #20 | Must be populated before any Entry Summary line carrying AD/CVD can be certified — CBP will reject on bad/missing rates |
| Idempotency on mutation routes | 8 of ~150 routes | OPEN-ITEMS #78 | CBP transmission routes specifically need this — a retried transmission must never double-file an entry |

**Bottom line:** the *data* side of Entry Summary is genuinely strong. Everything from "turn that data into CBP's wire format" outward — envelope, real transport, real response parsing — does not exist yet and is the actual engineering lift.

---

## 2. CATAIR chapters — full build scope, sequenced by build order

**2026-08-20: confirmed — build all chapters,** not a P0-only staged subset. CBP still certifies incrementally per transaction type in practice (you cannot skip straight to testing everything at once — see §3), so the table below is a **build/certification sequence**, not an in/out-of-scope filter. Two carrier-side chapters (Manifest, FTZ Admission) are flagged separately below since "build all those" most naturally reads as all the chapters this plan already scoped in — confirm explicitly if full carrier-side manifest functionality is also wanted, since that's a distinct product pivot (Qubere would be filing as/for a carrier, not just a broker-facing compliance tool) worth a deliberate yes rather than an assumed one.

| Sequence | Chapter | Why this order | Codebase readiness |
|---|---|---|---|
| 1 — foundational | Batch & Block Control | Required envelope for every other chapter; build once, first | **Codec built** (`src/lib/abi/batchBlockControl/`) — A/B/Y/Z, X0/X1 diagnostics, ACE-generated rejection fallbacks. Transport not wired. |
| 2 | Entry Summary Create/Update (AE/AX) | Core existing product capability; fastest path to a first certified transaction | **Codec built, expanding, DB integration layer wired (2026-08-21)** (`src/lib/abi/entrySummary/`) — input 10/11/40/50/89/90, output E0/E1, Appendix E check-digit validation, plus Bond Detail (31), FTZ Status (41), FTZ Privileged Foreign Status Detail (SE61), AD/CVD Case Detail (53), AD/CVD Duty Totals (88), Invoice Line Reference (42), Rulings (43), Commercial Description (44), License/Cert (52), Header/Line Entities (SE50/51/55/56), Importer's Additional Declaration (54), Header/Line Fees (34/62), Tax/Revenue (60/61), PSC Reasons (35), and Census Warning Override (CW02). **Database integration mapper built** (`src/lib/abi/entrySummary/fromCustomsFiling.ts`): maps Prisma `CustomsFiling` and its loaded relations (`ShipmentLineItem`, `CensusWarningOverride`, `FilingFeeLine`, `FtzDetail`, `AdcvdLineDetail`, `InvoiceLine`, `Bond`) to `EntrySummaryTransactionInput` and composes `buildAbiTransmissionForFiling()` end-to-end to raw batch/block bytes. Verified via round-trip decoding test (`tests/entry-summary-from-customs-filing.test.ts`). Explicit TODOs flagged for fields with no schema home (e.g. `cargoReleaseCertificationRequestIndicator`, `electronicInvoiceIndicator`, `shipmentUsageTypeCode`, `deferredTaxPaymentCode`, `consigneeNumber`, `articleSetIndicator`, `chargesAmount`, `grossShippingWeight`, AD/CVD specific-rate `quantity`). Transport not wired. |
| 3 | Entry Summary Query | Needed to check status of what's filed; reuses envelope + routing infra | **Codec built, expanded 2026-08-21** (`src/lib/abi/entrySummaryQuery/`) — input J0/J1/J2; output JA/JB/JC-JI plus (new) JK/JL/JM/JN bill/collection detail and the full Entry Summary Details Grouping. **Details Grouping confirmed as a direct reuse of `entrySummary/`'s own AE record specs** — not re-derived, backed by the PDF's own explicit statement (ESQ-52) that output 10-90 records "are identical to" the AE input records; `entrySummaryQuery/recordSpecs.ts` re-exports them directly. `parseQueryResponse` extended with a small state machine tracking the grouping structure (10 → 11? → [4A → 40 → 50+]+ → 89? → 90) per JB. Two real bugs caught: JK's and JN's test fixtures had swallowed real Interest/Paid/Principal Amount fields into their trailing filler (found via direct PDF extraction, not trusting the fixture) — fixed. **JJ (Protest Data) intentionally left unmodeled** — the completion-queue prompt described "5 records JJ-JN" but the actual test fixture only had real coverage for 4 of them (JK/JL/JM/JN); JJ was correctly left deferred rather than guessed. 9/9 slice tests; full ABI suite 948/948; full repo suite 3205/3205 (2 pre-existing skips). Transport not wired. |
| 4 | Cargo Release / Entry | Modern ACE filing bundles cargo release with entry summary; required to get goods released, not just paperwork accepted | **Codec built, expanded 2026-08-21** (`src/lib/abi/cargoRelease/`) — SE10/11/13/15/16/20/30/35/36/40/50/55/56/60/90 plus SE17 (Equipment), SE31/SE51 (Entity GBI, shared `EntityGbiInput` type), SE41/SE61 (FTZ detail). All 5 new records verified against the PDF and matched the test fixture exactly. **PGA grouping confirmed NOT independently modeled** — Cargo Release's own PDF lists PGA in its usage map but never defines PG0x field layouts itself; those live only in the PGA Message Set chapter (`src/lib/abi/pgaMessageSet/`), already built — verified via both PDFs, not assumed. **ISF grouping (SF10-SF36) remains deferred** — confirmed genuinely unique to this chapter's own PDF (not a reuse), but the test fixture has zero field assertions for it, so a ~15-record family was left unimplemented rather than guessed from scratch. 92/92 chapter tests, full ABI suite 944/944, full repo suite 3201/3201. Transport not wired. |
| 5 | Statement Processing (Daily/Periodic Monthly Statement) | Required to actually pay duties on filed entries — a production filer cannot operate without this | **Codec built** (`src/lib/abi/statement/`) — full Q1-Q7/QA/QE/QJ record set for both Daily and Periodic Monthly Statement flows (16 RecordSpecs). Deferred: SU (statement update/delete), RM/PN (ACH payment authorization), Outstanding Action ES Query Response grouping, printed-report rendering. All duty/tax/fee amounts confirmed 2-implied-decimal from the source PDF and bound to `Decimal`. Transport not wired. |
| 6 | Bond / eBond | Product has a `Bond` model; needs real verification, not just CRUD | **Codec built** (`src/lib/abi/ebond/`) — Records 10/12/20/30/35/36/40/45/46/90 (CB input / CX output). Deferred: interactive eBond Query (QB/QX), Trade Portal UI. Currency fields confirmed whole-dollar (0 implied decimals) — a genuine convention difference from Entry Summary/Statement's 2-implied-decimal fields, verified against the source PDF before implementing. Transport not wired. Existing `Bond` product model/verification logic is separate, still unconnected to this codec. |
| 7 | Drawback (TFTEA) | Strong existing foundation (claims/matches/lots are real) | **Codec built and corrected** (`src/lib/abi/drawback/`) — full 21-input/2-output backbone (Records 10, 31, 40-43, 50-53, 60-64, 70-73, 89-90, output E0/E1). Money fields bound to `Decimal` (Record 31's STB Bond Amount whole-dollar; Record 42/50/60/70 quantity fields 4-implied-decimal; Record 43/89/90 revenue amounts 2-implied-decimal; Record 64's Exchange Rate field **6-implied-decimal**, a new precision level for this codebase, verified via a dedicated round-trip test since a naive 2-or-4-decimal helper would silently truncate it), MMDDYY dates via `Date`, and ITIN/MTIN/ETIN cross-record join keys plus CBP ES Line #, Accounting Class Code, and Drawback Accounting Method Code via `numericCodeField` so leading zeros survive the Record 40↔52/72 and 50↔53/73 joins. **Data-quality history, resolved**: an initial implementation pass found Records 50, 51, 60, 61, 62, 63, 64, 70, and 71 (9 of 21) didn't match the real PDF layout — fabricated fields (a nonexistent "Export Tracking ID Number"/"Export Value Per Unit" on 60/70), a misplaced MTIN (belongs on 51, not 50), missing fields (Record 50's Action Indicator/Quantity/UOM/Factory Location; Record 63's Name of CBP Personnel; Record 64's Duty-Paid/Exchange-Rate/third-Tariff-Number), and transposed field order (61/71). All 9 were re-derived directly from `07-drawback-tftea-v27.pdf` (pp. 54-71, page-cited) and corrected in both `src/` and the two test files; the other 14 records were independently spot-checked and were already correct. 75/75 drawback tests, 434/434 full ABI suite, 2672/2672 full repo suite (2 pre-existing unrelated skips) — all green, typecheck/lint clean. Two judgment calls worth a second look: Record 64's Country of Export was implemented as class `AN` despite the PDF table literally saying `2S` (a filler class can't hold real "CA"/"MX" data — likely a table-extraction artifact); Record 51's MTIN was implemented as conditional (`C`) per the field table despite the narrative text calling it a join key. Transport not wired. |
| 8 | Partner Government Agencies (PGA) Message Set | Screening exists; filing transaction does not | **Codec built — all 35 records now covered** (`src/lib/abi/pgaMessageSet/`) — the 28 generic/mandatory cross-agency backbone records plus (2026-08-21) all 7 agency-specific variants: PG05/PG17 (FWS), PG23/PG28 (FDA), PG31/PG33 (NOAA/NMFS), PG35 (DOT/NHTSA). All 7 verified byte-for-byte against the PDF with zero discrepancies found. Existing PGA screening logic/`HtsPgaRequirement` data model untouched — this chapter is only the wire-format layer. Shared `dateFieldCCYY` primitive (from the original pass) not needed by any of the 7 new records — none use 8-char MMDDCCYY dates. New per-field conventions confirmed: PG28's Can Dimensions pack two significant-leading-zero 2-digit sub-values per field (`numericCodeField`); PG31's Net Weight and PG35's DOT Bond Amount are `Decimal` (2 and 0 implied decimals respectively, the latter reusing PG25's whole-dollar convention). The agency-specific test file had the same self-contained-fixture gap PGA's original test files had (fixed then) — reconciled again to exercise the real codec. 111/111 full PGA chapter tests, full ABI suite and full repo suite green apart from 2 unrelated pre-existing failures in `abi-entry-summary-query-parse.test.ts` from concurrent in-flight work on that chapter. Transport not wired. |
| 9 | In-Bond | Confirm actual customer demand for bonded-freight movements before building — cheap to sequence last if it turns out unneeded | **Codec built** (`src/lib/abi/inBond/`) — 13 records forming the full standard QP/WP/NS lifecycle (QP10/20/30/32/33 initiation, WP10/20 arrival-export-transfer, QT95/WT95 responses, NS10/30/40/50 status notification) out of a larger set; QP-Long BOL detail records (QP40/50/51/52/55/56/57/60/61/62/65/70/71/72/75/76), NS05, NS60, batch-control error echoes, and two rare action codes explicitly deferred. Independently spot-checked 6 of 13 records (QP10, QP20, QP30, WP10, WP20, NS30) byte-for-byte against the source PDF before implementing — clean, no fabrications, including confirming a genuinely mixed date-format chapter (QP20 is MMDDYY, WP20/NS30 are YYMMDD) and that NS30's "reserved for future use" block is real named fields, not filler in disguise. No new `fixedWidth.ts` primitive needed — the four already built (`dateField`, `dateFieldNumericMMDDYY`, `dateFieldYYMMDD`, `dateFieldCCYY`) covered every date format found. 351/351 chapter tests, full ABI suite 947/947, full repo suite green. Transport not wired. This closes out the originally-sequenced 9-chapter build. |
| — (added, not originally sequenced) | ACE Broker Download | Output-only manifest/bill-of-lading data push to ABI participants; source doc numbered 09 in the file set but sequenced here after the original 9 since it wasn't in the initial plan | **Codec built — complete, 27/27 records** (`src/lib/abi/brokerDownload/`). Final slice (2026-08-21) added the last 11 (2M, 1A, 2B, 4B, 2N, 3N, 4N, 1I, 2I, 2C, 0D) — two of them (2B, 0D) independently spot-checked against the PDF beforehand, including a genuinely duplicated "Carrier Code" field name (2B has two distinct secondary-notify-party fields sharing that label, not a fabrication) and 0D's tariff-code space-padding rule. No fixture bugs found this slice. 52/52 slice tests, full ABI suite 1086/1086, full repo suite 3351/3351. Transport not wired (output-only chapter, no input side to transmit). |
| — (added, not originally sequenced) | Importer/Bond Query (KI/KR) | Newly-discovered chapter — not in the original CATAIR document set. Confirmed as a distinct transaction from eBond Create/Update (CB/CX) via the authoritative Batch & Block Control application-identifier table (`01-batch-block-control-v23.pdf` pp. 11/23); source document `importer-bond-query-v7.pdf` (July 2025, Draft v7) located and downloaded live from cbp.gov, not previously in the source set | **Codec built** (`src/lib/abi/importerBondQuery/`) — all 9 records: K (input, up to 6 importer-number+address-request-code pairs per record), K1-K8 (output — bond type/amount/dates, name/address detail, Center ID, overflow). Independently verified against the PDF (pages 8-20), including a genuine printed-position-vs-class-width contradiction in K1's Bond Number field (PDF prints "71-89" but the field is only 9 chars wide, which would overlap the next field — correctly resolved as 71-79) and a genuine new date-class variant: K1's Bond Effective Date is class AN, not D or N — required a 5th shared date primitive, `dateFieldAlphanumericMMDDYY`, added surgically to `fixedWidth.ts`. 45/45 chapter tests, full ABI suite 1025/1025, full repo suite 3290/3290, typecheck/lint clean. Transport not wired. |
| — (added, not originally sequenced) | ACE Cargo Manifest/In-Bond/Entry Status Query ("CQ"/"C1") | Status-query counterpart to Entry Summary Query, for cargo/bill/in-bond status rather than entry summary status | **Codec built — all 26 records now covered** (`src/lib/abi/cargoManifestQuery/`) — the original 6-record core round trip plus (2026-08-21) all 20 remaining records (WO20/30/40/42/50/70/71/72, WR3/4/5, WS4/5, WSA/B/C/D, WN0/1). Every field cross-checked directly against the PDF, not just the test fixture. Real bug caught: WO71's positions 63-80 were labeled `Filler` in the test fixture but are actually a second PGA Reference ID Qualifier+Number pair (WO71 carries two ID pairs) — position math alone couldn't catch this since both readings sum to 80; only reading the PDF's actual field table did. Confirmed WO70/71/72 do NOT reuse `pgaMessageSet/` (different record family entirely — PG0x is line-level input data, WO70-72 is query-response status/disposition output, no field overlap). Several genuine per-field class inconsistencies within the chapter honored rather than "fixed": WO40's Manifested Quantity is class 8X (not N, unlike its sibling `quantity` field), WN1's Container Load Date is class 6AN (not N, unlike every other date in the chapter), WO42's In-Bond Entry Type is 2N while the same-named field elsewhere in the chapter is 2AN. `parseCargoManifestQueryResponse` extended to walk and group all 20 new records, not just classify them. 7/7 slice tests; full chapter 73/73; full ABI suite 948/948; full repo suite 3205/3205. Transport not wired. |
| Confirm before building | Manifest (ocean/air/rail/truck) | Carrier-side filing; no product signal today that Qubere acts as a carrier — this is a different business, not just another chapter | 0% |
| Confirm before building | Foreign Trade Zone Admission (e214) | No FTZ functionality anywhere in the product today | 0% |

**Sequencing rationale:** each chapter after #1 reuses the envelope built in #1 and (from #3 on) the transport/response-parsing infrastructure built in #2 — so the *marginal* build cost drops sharply after the first two are proven, even though the total scope is now "all of it."

**Reference/validation data (not wire-format chapters — no codec to build, just lookup tables):** `src/lib/abi/governmentAgencyCodes.ts` (Appendix V, June 2020) — all 53 CATAIR Government Agency Codes with full agency names, keyed for `isValidGovernmentAgencyCode`/`getGovernmentAgencyCodeEntry` lookups; validates PGA Message Set's PG01 `governmentAgencyCode` field (not yet wired into `pgaMessageSet/`'s own validation — available for that or for UI-level validation whenever needed). Independently verified complete and correct against the source PDF (full re-extraction, not spot-check, since only 4 pages) — every entry matches, including two genuine non-alphabetical orderings in CBP's own document (DTC before DOL, IDV before ICE) correctly preserved rather than "fixed." Antigravity wrote the `src/` module directly this time, not just tests, since there's no codec/position-math judgment involved — a structured lookup table extracted from a short, clean source doesn't carry the same fabrication risk as position-derived wire-format records. `src/lib/abi/errorDictionary.ts` (the 1055-row CBP condition-code dictionary, `10-error-dictionary-2026-07.xlsx`) — `ABI_ERROR_DICTIONARY_ROWS` (all 1054 data rows verbatim), plus `getAbiError(code)`/`getAllAbiErrors(code)` lookups. Condition codes are **not globally unique** — 27 codes (e.g. "003", "004", "60D") mean different things depending on query context (Census Warning Query vs. Entry Summary Query vs. AD/CVD Query, etc.) — correctly handled via `ABI_ERROR_DICTIONARY_ALL: Map<string, Entry[]>` returning every match rather than silently dropping the ambiguous ones; the single-entry `ABI_ERROR_DICTIONARY` convenience map keeps the *last* occurrence for ambiguous codes (its doc comment originally claimed "first occurrence," which was backwards given `new Map()` construction semantics — fixed). Independently verified via a full ordered row-by-row diff against the source spreadsheet (not spot-check, since it's structured data cheap to fully re-extract): 1053 of 1054 rows matched exactly, one harmless `null`-vs-`""` normalization difference on a row marked `"<eliminated>"` (deprecated code). Not yet cross-wired into any of the diagnostic/output records already built (X0/X1, E0/E1, WR0, WO60's disposition codes) — available for that whenever useful. `src/lib/abi/validCodes.ts` (Appendix B, Valid Codes, scoped-down first pass — Entry Type Codes p.26, Mode of Transportation Codes p.27, EU Country Codes p.17; the rest of Appendix B's 31 pages not yet extracted) — `isValidEntryTypeCode`/`getEntryTypeCodeEntry` (47 entries: 7 category headers + 40 real codes, with automated-filing-eligibility annotations `*`/`**`/`***` mapped to a `filingStatus` field — useful for validating Entry Summary's/Cargo Release's entry type fields against which codes are actually approved for automated filing, not just which codes exist), `isValidModeOfTransportationCode`/`getModeOfTransportationCodeEntry` (15 entries), `isEuCountryCode`/`getEuCountryCodeEntry` (27 entries, not originally requested but a reasonable inclusion since it was on a page Antigravity was already extracting). Fully re-verified against the source PDF (all three tables, not spot-check) — zero errors across all 47+15+27 entries. This is the third reference-data extraction in a row with an essentially clean result (Government Agency Codes 53/53, Error Dictionary 1053/1054, Valid Codes 89/89) — Antigravity's structured-data extraction quality has been consistently higher than its wire-format position-derivation work, which makes sense given the lower fabrication surface. `src/lib/abi/countryCurrencyCodes.ts` (Appendix B, Country and Currency Codes, pp. 6-16, the largest table — 243 countries/256 currency mappings). Spot-checked (not full re-extraction, given size) on every genuinely ambiguous case flagged in Antigravity's own report — all 6 confirmed correct: Burma/Myanmar's table-vs-changelog conflict (table still says MM despite a 2016 changelog claiming BU replaced it — correctly trusted the table), IMF's blank country code + non-ISO footnote, Kosovo's CBP-specific KV (not ISO's XK), Switzerland's 3-currency row (CHF/CHW/CHE), Slovakia's SKK-to-EUR entry that literally spans the page 13/14 boundary, and the BCEAO/BEAC footnote-to-country mappings. `src/lib/abi/equipmentDescriptionCodes.ts` (Appendix B, pp. 28-31, 133 codes) — 5 of 6 spot-checked entries confirmed exact including two with complex wrapped/punctuated descriptions; one (`CX`) unconfirmed because the verifier's own PDF pull got cut off at a page boundary, not a suspected error — flagged for a cheap follow-up look, not distrusted. `src/lib/abi/unitsOfMeasure.ts` (Appendix B, Units of Measure, pp. 22-25, 255 codes, 30 flagged `isUpdatedOrNew` for ACS-vs-ACE version differences). Two small, non-blocking issues found on spot-check and left as-is rather than round-tripped back to Antigravity: `isUpdatedOrNew: true` entries also keep a redundant literal `*` in their `description` string; `TN`'s description dropped a parenthetical qualifier (`Tin ("Nodules")` → just `"Tin"`). Neither is a fabrication or position error. **Appendix B is now fully extracted, closing out all of `docs/plans/ANTIGRAVITY-PROMPT-QUEUE.md` P1-P6.** `src/lib/abi/locationCodes.ts` (Appendix B pp. 18-21, 102 entries: 57 US state/territory + 3 Armed Forces codes, 32 Mexican states, 13 Canadian provinces) — spot-checked and confirmed, including a genuine CBP typo ("RI — Rhoda Island") correctly preserved verbatim rather than silently corrected. `src/lib/abi/tariffAbbreviations.ts` (Appendix C, Tariff Abbreviations, 100 entries) — spot-checked and confirmed, including another preserved source typo ("GBQ — Giqabecquerel"). `src/lib/abi/censusCodes.ts` (Appendix H, Census Warning & Override Codes, 2008 doc — 30 override codes + 14 warning conditions with their allowed-override-code sets) — spot-checked and confirmed, including the two narrowest warning conditions (27P/27Q, restricted to override code 51 only). Appendix F (Duty Calculation, 2011) remains deliberately un-queued pending Client-Rep verification.

---

## 3. Phased execution plan

### Phase 0 — Business & legal setup (no code)
- [x] Resolve the narrower Open Question A sub-question — **software-vendor path chosen**: Qubere's customers are the brokers/filers of record, Qubere transmits under each customer's own credentials.
- [x] Send CBP initial outreach / Letter of Intent — sent 2026-08-21, awaiting response.
- [ ] Get a Client Representative assigned (pending CBP's response to the above).
- [ ] Confirm with the Client Rep: current CATAIR chapter list, current revision numbers, and CBP's approved transport options (VAN/AS2/direct connect) for a new vendor in 2026.
- [ ] Confirm what CBP requires evidence-wise from Qubere as the software vendor vs. from each customer-broker's own filer relationship — this determines what `RealAceProvider`'s credential model needs to support (per-customer filer credentials, not a single Qubere-wide credential).
- [ ] Get CBP ABI test-environment credentials once the above is settled.

### Phase 1 — Foundational transmission infrastructure (engineering)
- [x] Build the **Batch & Block Control envelope** module — CBP's required header/trailer wrapper, shared by every chapter transaction. Already built as part of the CATAIR chapter work, not this phase specifically: `src/lib/abi/batchBlockControl/` (A/B/Y/Z wrap/unwrap, X0/X1 diagnostics, ACE-generated rejection fallbacks). Retroactively checked off here since it satisfies this bullet; see §2 sequence 1.
- [x] Build the real CATAIR fixed-length record serializer, rescoped from the existing U9 spec. Same as above — already satisfied by `src/lib/abi/entrySummary/` (and every other chapter codec), which serializes against CBP's real published field positions, not the old internal `catair-ae-2024.1` reference layout the U9 spec targeted.
- [~] Implement `RealAceProvider` (`src/lib/filing/transmissionProvider.ts`) — **credential handling done 2026-08-21**, real transport still open. Correction: the "existing secret-ref pattern from `FilerProfile.transportConfig`" this bullet used to point to does not exist — verified by grep, no such model/field anywhere in the codebase. What got built instead: a new per-Account `AbiFilerCredential` Prisma model (filer code + an opaque `secretRef` pointer, no raw secret in Postgres — deliberately not copying `IntegrationConfig`'s existing plaintext `apiKey`/`apiSecret` columns, which aren't adequate for real federal filer credentials) plus a pluggable `SecretStoreResolver` interface (`src/lib/filing/abiCredentialResolver.ts`) that throws until a real secret store is wired in. `RealAceProvider`'s constructor now takes a `RealAceProviderConfig` (per-customer credential + resolver) instead of reading global `process.env.CBP_ABI_FILER_CODE/PASSWORD`. **Migration gap fixed 2026-08-21:** `prisma db push` had synced the dev database directly, with no migration file generated for `AbiFilerCredential`. Baselined via `prisma/migrations/20260822100000_add_abi_filer_credential/` (guarded `IF NOT EXISTS`/`DO $$` SQL, same pattern as `20260821000000_add_customs_version_tables`) + `prisma migrate resolve --applied`, so migration history now matches the live schema without re-executing DDL against it. `prisma migrate status` confirms clean. `transmit()`/`getStatus()` remain throwing stubs — still blocked on Phase 0 (no CBP test credentials, no Client Rep confirmation of transport method; see `docs/plans/PHASE1-PROMPT-QUEUE.md`'s intro for CBP's actual transport menu — MQIPT/IPSEC-IKEv2/MPLS-VPN/Service-Center/VAN, not "VAN/AS2/direct connect" as this bullet used to say).
- [x] Build real CBP response/reject parsing — done 2026-08-21 for `RealAceProvider.parseAcknowledgment` specifically: it now classifies raw output into batch/block-level rejections (X0/X1, resolved through the full `errorDictionary.ts`) vs. Entry Summary transaction-level responses (delegated to `parseEntrySummaryResponse`/`interpretEntrySummaryResponse`), assembling already-built, already-PDF-verified pieces — no new record layout was derived. **Known limitation, not a defect:** `AcknowledgmentResult` has a single flat `referenceNumber` field, so if a raw payload ever contains multiple Entry Summary transactions, only the first one's reference number surfaces — acceptable for today's one-transaction-per-acknowledgment usage, worth revisiting if that assumption changes. `devStub.ts`'s simulated `ACCEPTED`/`CANCELLED` responses are untouched and still the active dev-mode behavior — this bullet's original "replace devStub.ts" framing was about the CBP/ABI procedure path specifically, and nothing calls `parseAcknowledgment` from any live route yet (confirmed by grep), so there's nothing to cut over until real transport exists.
- [x] Idempotency on transmission-adjacent routes — done 2026-08-21 for the filing/transmission set specifically (not the broader ~150-route gap tracked separately in OPEN-ITEMS #78): `POST /api/filing`, `PATCH /api/filing/[id]`, `POST /api/filing/[id]/validate`, `PATCH .../currency`, `PATCH .../declaration`, `POST /api/shipments/[id]/pipeline-retry`, on top of the four routes (`transmit`/`cancel`/`approve`/`resubmit`) that already had it. Verified: same `checkIdempotency`/`persistIdempotency` pattern applied correctly at every return point, 4 new tests plus the full 3383-test repo suite green.

### Phase 2 — Chapters 2-3 build + first certification pass
- [x] Entry Summary Create/Update DB Integration: built `fromCustomsFiling` mapper (`src/lib/abi/entrySummary/fromCustomsFiling.ts`) connecting Prisma `CustomsFiling` and its gap-closure relations (`ShipmentLineItem`, `CensusWarningOverride`, `FilingFeeLine`, `FtzDetail`, `AdcvdLineDetail`, `InvoiceLine`, `Bond`) to `EntrySummaryTransactionInput` and composed into end-to-end `buildAbiTransmissionForFiling()` pipeline. Verified with round-trip decoding test (`tests/entry-summary-from-customs-filing.test.ts`). Transport remains deferred.
- [x] Entry Summary Query DB Integration — done 2026-08-22: `src/lib/abi/entrySummaryQuery/fromCustomsFiling.ts` (CustomsFiling + AbiFilerCredential → EntryReference[] → assembled + enveloped query bytes) and `interpretResponse.ts` (parsed query response → error-dictionary-enriched conditions + a separate `EntrySummaryQueryStatusSummary` structured object for the liquidation/bond/revenue detail that doesn't fit `CustomsResponse`'s flat shape, which was confirmed to need no schema changes). **Two real bugs found in review and fixed, not just cosmetic:** (1) the envelope's `applicationIdentifierCode` defaulted to `"JC"` — that's actually the Entry Summary Status Detail *output record's* own prefix, an unrelated namespace; this project's own verified reference table (`batchBlockControl/applicationIdentifierCodes.ts`) says Entry Summary Query's real code is `"EQ"`, matching the established pattern in the Create/Update chapter's equivalent (`"AE"`). Slipped past the tests because the wrong value was baked into the test fixture itself rather than exercising the default. (2) `interpretResponse.ts` imported `ParsedQueryResponse`/`EntrySummaryQueryResult` from `./types`, but those types actually live in `./parse` — a real `tsc` error, invisible to `vitest` because its esbuild-based transform doesn't type-check across module boundaries for type-only imports. Both fixed; full repo suite (3394/3394 + 2 pre-existing skips) and `tsc --noEmit` clean after the fix. Transport remains deferred, same as Create/Update.
- [x] Populate AD/CVD rate data properly (OPEN-ITEMS #20) — resolved by an earlier, unrelated pass; `prisma/seed.ts` now seeds real AD/CVD orders and Section 301 rates with Federal Register citations. OPEN-ITEMS.md already marks #20 `[RESOLVED]`.
- [ ] Run CBP's prescribed test scenarios with the Client Rep for these two chapters; iterate on rejects.
- [ ] Target: CBP grants production authorization for Entry Summary Create/Update + Query.

### Phase 3 — Chapters 4-8 expansion (full remaining scope)
- [x] Cargo Release / Entry — **transaction assembly done 2026-08-22**: `src/lib/abi/cargoRelease/assembleTransaction.ts` sequences all 19 input records (SE10-SE61) per the chapter's own "Input Record Usage Map for 'A' Add and 'R' Replace Actions" (PDF page 24 / SE-22). Independently re-verified against the actual PDF table (not just the report) — sequencing and every repeat limit (SE15 1-3, Conveyance/Equipment/References 0-99, header entities 0-12, GBI/street 0-3, line entities 0-11, HTS lines 1-32) check out exactly. Two harmless, low-severity gaps noted, not fixed (neither causes incorrect behavior): the 999 bill-group cap isn't shown on the cited page itself (confirmed instead via the "U" Update action's map on SE-25 in the same doc), and the 9999 line-item cap wasn't visible/confirmed on either page read. 107/107 tests, `tsc` clean.
  **Response parsing also done 2026-08-22**: `src/lib/abi/cargoRelease/parseResponse.ts` walks a raw output message per the chapter's "Output Record Usage Map" (PDF pages 28-29 / SE-26/27) and attaches record-level SE90 errors to their specific occurrence (bill, conveyance, entity, line, etc.) rather than one flat list. Independently re-verified against the real PDF, including reading ahead to page 85 (footer-labeled SE-83, cited in the report as "page 83" — a benign off-by-2 between the doc's own internal page numbers and absolute PDF page count, content itself is accurate) for the SE90 record's own two-type design: message-level codes 01/02/03/04 (Rejected/Accepted/Accepted-with-Warning/Referred-to-Human-Review) vs. record-level codes 11/13 (Record Rejected/Record Accepted with Warning) — confirmed word-for-word against the source, correctly implemented. This also incidentally cross-confirms the request-side "9999 line-item cap" flagged as unverified above was likely wrong in spirit — the output side's equivalent "Simplified Entry Line Grouping" caps at 999, not 9999. 115/115 tests, `tsc` clean.
  **Multi-transaction gap fixed 2026-08-22**: `ParsedCargoReleaseResponse` restructured to `{ scenario, headerGroups: ParsedCargoReleaseHeaderGroup[], unrecognizedLines }` — each `headerGroups[]` element is a fully self-contained transaction (its own header/additionalHeader/contactCancellation, bills, references, entities, lines, and disposition), matching the real "SE Header Grouping | M | 999" repeat. A new SE10 line correctly starts a new group and finalizes the previous one; an overall aggregate `scenario` is derived across all groups (REJECTED > REFERRED_TO_HUMAN_REVIEW > ACCEPTED_WITH_WARNINGS > ACCEPTED priority — an app-level convenience choice, not a CBP-defined value). Independently verified the fix is real, not cosmetic: the new test builds two genuinely distinct transactions via the already-verified `assembleTransaction()` (different entry numbers, bill numbers, countries of origin, one accepted/one rejected), concatenates their raw output, and confirms both come back correctly separated rather than merged or overwritten. No other callers existed yet to update (confirmed by grep). 116/116 chapter tests, full repo suite 261 files / 3409 tests / 2 pre-existing skips, `tsc` clean.
  **DB integration done 2026-08-22, with 5 real bugs found and fixed on independent review** — `src/lib/abi/cargoRelease/fromCustomsFiling.ts` (request side) and `interpretResponse.ts` (response side, clean on review — only touches already-verified types plus the error dictionary, no Prisma fields, and its one real judgment call — using the master ACE Error Dictionary since the "Cargo Release Condition Codes" document the SE90 spec separately mentions isn't in this codebase — is disclosed and defensible). The request side was a different story: traced every field in its hand-written Prisma-relation type against the actual schema and found 5 that don't exist or are misnamed — `ShipmentLineItem.shipmentParties` (no such relation anywhere; `ShipmentParty` only scopes to `shipmentId`, so SE50/51/55/56 line-level entities were permanently dead code), `ShipmentTrackingIdentifier.scac` (real field is `issuer`), `Shipment.trackingNumber` (doesn't exist, a dead fallback), `FtzDetail.zoneStatus` and `.ftzLineItemQuantity` (real fields are `merchandiseStatusCode` and `lineItemQuantity` — confirmed against `entrySummary/fromCustomsFiling.ts`'s own already-verified FTZ mapping, which uses those exact names for the same purpose). All five were invisible to the tests because the mock fixture used the identical invented field names — fully self-consistent, disconnected from reality, exactly the failure mode this project's own history has repeatedly flagged as its biggest risk. Fixed directly (mechanical renames): corrected all 5 references, updated the test fixture, replaced the fabricated line-level-entity mapping with an explicit TODO (matching how `contactName`/`contactPhone` were already correctly handled), dropped the dead `trackingNumber` fallback. Re-verified after the fix: `tsc --noEmit` clean, chapter suite 124/124, full repo suite 263 files / 3417 tests / 2 pre-existing skips.
- [~] Statement Processing (daily/periodic statement + payment) — **Daily Statement response parsing done 2026-08-22**: `src/lib/abi/statement/parseDailyStatement.ts` walks CBP-generated Daily Statement output per the chapter's own "Output Record Structure Map" (PDF page 9). Unlike every other Phase 3 chapter checked so far, this chapter's core records (Q1/Q2/QA/Q3/Q4/QE/Q5/Q6/QJ/Q7) are entirely CBP-generated output — confirmed directly from the source PDF's own introduction — so there is no filer-built input transaction to assemble here, only a response parser (mirroring Cargo Release's `parseResponse.ts`, not its `assembleTransaction.ts`). Independently re-verified clean, no bugs found: the structure map matches the PDF exactly, and the claim that this chapter has no SE90-style per-record error/condition records was confirmed by grepping the full 42-page source PDF myself (only narrative prose mentions of "error"/"reject," no actual output error record). 89/89 chapter tests, full repo suite 264 files / 3426 tests / 2 pre-existing skips, `tsc` clean. **Periodic Monthly Statement response parsing also done 2026-08-22**: `src/lib/abi/statement/parsePeriodicStatement.ts`, same output-only shape as Daily (confirmed directly from its own separate source PDF's introduction), but with three real, independently-verified differences from Daily that the implementation correctly handles rather than copy-pasting: application identifier `MS` not `PF` (cross-checked against `applicationIdentifierCodes.ts`), 9,999 repeat limits not 2,000 (correctly enforced with proper boundary checks), and QA/QE/QJ fee records genuinely Mandatory not Conditional (strict-by-default with an explicit, tested `allowMissingFees` opt-in). Also independently verified the shared reuse of `Q7DeletedInput`/`decodeQ7Deleted` between the two chapters is genuinely correct, not a shortcut — pulled Periodic's own Q7 record page from the PDF and confirmed it's byte-for-byte identical to the existing shared spec. 102/102 chapter tests, full repo suite 265 files / 3439 tests / 2 pre-existing skips, `tsc` clean. **DB integration (response side) done 2026-08-22, correct but with a real semantic gap worth flagging clearly:** `src/lib/abi/statement/interpretDailyStatement.ts` and `interpretPeriodicStatement.ts` map parsed responses to `CustomsResponseRecordData[]` via a `filingIdMap`, plus a returned-but-not-yet-persisted structured summary object for statement-level totals (correctly recommended reusing the existing `StatementRecord`/`StatementFeeLine` Prisma models — confirmed real, at `prisma/schema.prisma` ~line 6370 — for future persistence work rather than inventing a new one). Daily's per-entry attribution is genuinely correct (`Q1DailyInput` really does carry `entryFilerCode`/`entryNumber`). **Periodic's is not the same granularity, despite the walkthrough's summary describing it in parallel terms as "per-entry."** Independently checked: `Q1PeriodicInput` has no `entryFilerCode`/`entryNumber` at all — it only has `periodicDailyStatementFilerCode`/`periodicDailyStatementNumber`, because Periodic's Q1 represents one rolled-up *daily statement* (which can itself contain many entry summaries), not a single entry summary the way Daily's does. The code handles this honestly (attributes via a statement-number composite key, tested explicitly including the `defaultFilingId` fallback) rather than fabricating entry-level data it doesn't have — not a bug. But any real caller assuming Periodic's `filingIdMap` should be keyed by entry number (the natural assumption, matching Daily's convention) would find every Periodic Q1/Q2/QA `CustomsResponse` row silently falling through to `defaultFilingId` instead. Worth resolving deliberately (e.g., should these rows even be per-filing at all, or belong only in the summary object?) before this is wired into anything real — not fixed this round, flagged for the next pass. 111/111 chapter tests, full repo suite 267 files / 3448 tests / 2 pre-existing skips, `tsc` clean. **SU/RM/PN (deliberately deferred, per the "Outstanding Action ES Query Response Grouping" being out of scope) remain open.** See `docs/plans/PHASE3-PROMPT-QUEUE.md`.
- [ ] Bond/eBond real verification (replace `status: "Unverified"` placeholder with actual surety/ACE-backed verification).
- [ ] Drawback CATAIR transaction on top of the existing claim/match/lot models.
- [ ] PGA Message Set filing transaction on top of existing screening.
- [ ] In-Bond, once demand is confirmed per the chapter-9 note above.
- [ ] Manifest / FTZ Admission, only if explicitly confirmed as in scope (see §2) — otherwise skip.
- [ ] Repeat CBP test-and-certify cycle per chapter added.

### Phase 4 — Production cutover
- [ ] Get listed on CBP's ABI Software Vendor list (if software-vendor path) or activate production filer code (if self-filer path).
- [ ] Cut over from `devStub.ts` simulation to `RealAceProvider` in production, behind the existing `CUSTOMS_FILING_MOCK_RESPONSES` flag pattern (`devStub.ts:85`) so it's a single, reversible switch.
- [ ] Remove the `[DEMO MODE]` simulated-filing path currently writing `triageState: "APPROVED"` with no environment gate (`src/modules/agents/customsFilingAgent.ts:176`, OPEN-ITEMS #4) — this must not coexist with real production transmission.

### Phase 5 — Ongoing compliance (standing obligation, not a milestone)
- [ ] Monitor CATAIR revision releases (CBP revises chapters multiple times a year) and re-test changes before they hit production, per CBP's stated expectation that vendors self-test subsequent programming changes.
- [ ] Track ABI performance/error-rate standards under 19 CFR 143 Subpart A; CBP can suspend privileges for a poor rate.
- [ ] Maintain the audit trail (`AuditLog`) coverage gap (OPEN-ITEMS #19 — ~45% of write routes) at least across every transmission-adjacent route, since this is now regulator-facing, not just internal audit.

---

## 4. Source documents

**Downloaded 2026-08-20 into [`docs/plans/catair-source-docs/`](catair-source-docs/)** — pulled live from cbp.gov (a plain browser-UA `curl` works fine; only our automated WebFetch tool was 403'd, not the site itself) and cross-checked against the hub page's actual current link targets, not search-index snapshots. This is now the authoritative local copy — build against these, not the earlier search-derived links in prior revisions of this doc.

| Local file | Chapter | Live revision found |
|---|---|---|
| [00-abi-requirements-overview.pdf](catair-source-docs/00-abi-requirements-overview.pdf) | ABI Requirements (general/overview) | 2022-08 |
| [01-batch-block-control-v23.pdf](catair-source-docs/01-batch-block-control-v23.pdf) | Batch & Block Control | v23, 2023-06 |
| [02-entry-summary-create-update-2026-07.pdf](catair-source-docs/02-entry-summary-create-update-2026-07.pdf) | Entry Summary Create/Update (AE/AX) | 2026-07-17 — newer than anything search surfaced |
| [03-entry-summary-query-2026-05-v26.pdf](catair-source-docs/03-entry-summary-query-2026-05-v26.pdf) | Entry Summary Query | v26, 2026-05 |
| [04-cargo-release-implementation-guide-v40.pdf](catair-source-docs/04-cargo-release-implementation-guide-v40.pdf) | Cargo Release Implementation Guide | v40, 2025-07 |
| [04b-cargo-manifest-bond-entry-status-query-v21.pdf](catair-source-docs/04b-cargo-manifest-bond-entry-status-query-v21.pdf) | Cargo/Manifest/Bond/Entry Status Query | v21, 2025-09 |
| [05-daily-statement.pdf](catair-source-docs/05-daily-statement.pdf) | Statement Processing — Daily Statement | 2025-09 |
| [05b-periodic-monthly-statement.pdf](catair-source-docs/05b-periodic-monthly-statement.pdf) | Statement Processing — Periodic Monthly Statement | 2020-02 (oldest of the set — worth double-checking with the Client Rep for a newer revision) |
| [06-ebond-create-update-v1.9.pdf](catair-source-docs/06-ebond-create-update-v1.9.pdf) | eBond Create/Update | v1.9, 2020-04 |
| [06b-in-bond-v51-2026-04.pdf](catair-source-docs/06b-in-bond-v51-2026-04.pdf) | In-Bond | rev 51, 2026-04 |
| [07-drawback-tftea-v27.pdf](catair-source-docs/07-drawback-tftea-v27.pdf) | Drawback (TFTEA) | v27, 2025-07 |
| [08-pga-message-set-2026-07.pdf](catair-source-docs/08-pga-message-set-2026-07.pdf) | Partner Government Agencies (PGA) Message Set | 2026-07 |
| [09-broker-download-draft.pdf](catair-source-docs/09-broker-download-draft.pdf) | ACE Broker Download | draft, 2026-07 |
| [10-error-dictionary-2026-07.xlsx](catair-source-docs/10-error-dictionary-2026-07.xlsx) | CATAIR Error Dictionary (all reject/error codes — needed for Phase 1's response parsing) | 2026-07, `.xlsx` not PDF |
| [appendix-b-valid-codes.pdf](catair-source-docs/appendix-b-valid-codes.pdf) | Appendix B — Valid Codes | 2026-08 |
| [appendix-c-tariff-abbreviations.pdf](catair-source-docs/appendix-c-tariff-abbreviations.pdf) | Appendix C — Tariff Abbreviations | 2016 (via a 2020 reissue) |
| [appendix-e-valid-entry-numbers.pdf](catair-source-docs/appendix-e-valid-entry-numbers.pdf) | Appendix E — Valid Entry Numbers | 2012 |
| [appendix-f-duty-calculation.pdf](catair-source-docs/appendix-f-duty-calculation.pdf) | Appendix F — Duty Calculation | 2011 — old; sanity-check against `src/lib/tariff/dutyEngine.ts`'s current logic before treating as authoritative |
| [appendix-h-census-codes.pdf](catair-source-docs/appendix-h-census-codes.pdf) | Appendix H — Census Codes | 2018 |
| [appendix-v-government-agency-codes.pdf](catair-source-docs/appendix-v-government-agency-codes.pdf) | Appendix V — Government Agency Codes | 2020 |

**Deliberately not downloaded:**
- **Manifest / FTZ Admission (e214)** — per §2, "confirm before building"; the hub lists a Truck Manifest section (`/trade/automated/catair/air-features` and similar) and a full FTZ chapter, both skipped pending an explicit yes.
- **Agency-specific PGA supplements** (FDA, DEA, ATF, USFWS, NMFS, DDTC, CPSC, APHIS, etc.) — the hub page lists ~15 of these. The core `pga-message-set` chapter above is the format spec; each agency's supplement only matters once we know which PGAs actually apply to Qubere customers' commodities. Pull the relevant ones when that's known.
- **~90 other guidance/technical-documentation pages** on the hub (reconciliation, ISF, currency exchange, MID, quota query, census warning query, AD/CVD case query, and more) — out of the chapters scoped in §2. The hub page was fully crawled during this pass, so revisiting it later for any of these is a five-minute lookup, not a re-discovery effort.

**Process/reference pages (not chapter documents, but load-bearing for Phase 0):**

| What | Link |
|---|---|
| Getting started / how to transmit via EDI (first point of contact = CBP Client Representative) | https://www.cbp.gov/trade/automated/getting-started/transmitting-data-cbp-electronic-data-interchange-edi |
| ABI Software Vendors list (current certified vendors — reference for the software-vendor path in Open Question A) | https://www.cbp.gov/document/guidance/abi-software-vendors-list |
| 19 CFR Part 143 Subpart A — Automated Broker Interface (the regulation itself) | https://www.ecfr.gov/current/title-19/chapter-I/part-143/subpart-A |

---

## 5. Immediate next actions (this week)

1. ~~Confirm the self-filer vs. software-vendor sub-question~~ — done, software-vendor path.
2. ~~Send the Letter of Intent to CBP~~ — done 2026-08-21, awaiting response.
3. **Waiting on CBP's response** to get a Client Representative assigned — this is the current blocker; nothing in Phase 1 (real transport) can be built against a confirmed spec until the Client Rep confirms transport options and current chapter revisions.
4. While waiting: have the Client Rep, once assigned, confirm the Periodic Monthly Statement (2020) and Duty Calculation appendix (2011) revisions specifically — those are the two oldest documents in the downloaded set and are the most likely to have quietly drifted from what's live today.
5. While waiting: the codec/reference-data track (§2) is essentially done and doesn't need CBP's response to continue if there's more appetite for it — but the highest-leverage next engineering step once CBP responds is Phase 1's `RealAceProvider`, which now specifically needs a **per-customer credential model** (each broker-customer's own filer code/credentials), not a single Qubere-wide one, given the software-vendor path.

## Open items this plan does not resolve

- Whether any current Qubere customer's broker-of-record relationship changes if Qubere becomes a direct transmitter for that customer — a contracts/customer-success question, not engineering.
- Staffing: does Qubere have or need a licensed customs broker on staff under the self-filer path (Open Question A).
- Whether Manifest / FTZ Admission are actually wanted (flagged in §2 as "confirm before building," not assumed in scope from "we will build all those").
